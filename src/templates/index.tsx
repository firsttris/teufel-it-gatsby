import Link from 'gatsby-link';
import * as React from 'react';
import { FormattedMessage } from 'react-intl';

const Index = () => (
  <div>
    <FormattedMessage id="PRINCIPLES_TDD_TEXT" />
  </div>
);

export default Index;
